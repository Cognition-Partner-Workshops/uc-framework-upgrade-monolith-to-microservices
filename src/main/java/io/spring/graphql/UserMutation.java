package io.spring.graphql;

import com.netflix.graphql.dgs.DgsComponent;

// TODO: User mutations (createUser, login, updateUser) have been extracted to the User Service.
// The User Service handles all user-related GraphQL mutations.
// Remove this placeholder once the monolith no longer needs to reference these mutations.
@DgsComponent
public class UserMutation {}

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.articleservice.infrastructure.mybatis.mapper.ArticleMapper">
    <insert id="insert">
        insert into articles(id, user_id, slug, title, description, body, created_at, updated_at)
        values(
          #{article.id},
          #{article.userId},
          #{article.slug},
          #{article.title},
          #{article.description},
          #{article.body},
          #{article.createdAt},
          #{article.updatedAt}
        )
    </insert>
    <update id="update">
        update articles
        <set>
            <if test="article.title != null">title = #{article.title},</if>
            <if test="article.slug != null">slug = #{article.slug},</if>
            <if test="article.description != null">description = #{article.description},</if>
            <if test="article.body != null">body = #{article.body},</if>
            updated_at = #{article.updatedAt}
        </set>
        where id = #{article.id}
    </update>
    <delete id="delete">
        delete from articles where id = #{id}
    </delete>
    <select id="findById" resultMap="article">
        select
          A.id articleId,
          A.user_id articleUserId,
          A.slug articleSlug,
          A.title articleTitle,
          A.description articleDescription,
          A.body articleBody,
          A.created_at articleCreatedAt,
          A.updated_at articleUpdatedAt,
          T.name tagName
        from articles A
        left join article_tags AT on A.id = AT.article_id
        left join tags T on AT.tag_id = T.id
        where A.id = #{id}
    </select>
    <select id="findBySlug" resultMap="article">
        select
          A.id articleId,
          A.user_id articleUserId,
          A.slug articleSlug,
          A.title articleTitle,
          A.description articleDescription,
          A.body articleBody,
          A.created_at articleCreatedAt,
          A.updated_at articleUpdatedAt,
          T.name tagName
        from articles A
        left join article_tags AT on A.id = AT.article_id
        left join tags T on AT.tag_id = T.id
        where A.slug = #{slug}
    </select>
    <select id="findTag" resultType="io.spring.articleservice.core.article.Tag">
        select id, name from tags where name = #{tagName}
    </select>
    <insert id="insertTag">
        insert into tags(id, name) values(#{tag.id}, #{tag.name})
    </insert>
    <insert id="insertArticleTagRelation">
        insert into article_tags(article_id, tag_id) values(#{articleId}, #{tagId})
    </insert>
    <resultMap id="article" type="io.spring.articleservice.core.article.Article">
        <id column="articleId" property="id"/>
        <result column="articleUserId" property="userId"/>
        <result column="articleSlug" property="slug"/>
        <result column="articleTitle" property="title"/>
        <result column="articleDescription" property="description"/>
        <result column="articleBody" property="body"/>
        <result column="articleCreatedAt" property="createdAt"/>
        <result column="articleUpdatedAt" property="updatedAt"/>
        <collection property="tags" ofType="io.spring.articleservice.core.article.Tag">
            <result column="tagName" property="name"/>
        </collection>
    </resultMap>
</mapper>

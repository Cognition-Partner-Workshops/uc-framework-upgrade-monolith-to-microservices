<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.userservice.infrastructure.mybatis.readservice.UserRelationshipQueryService">
    <select id="isUserFollowing" resultType="boolean">
        select count(1) > 0 from follows where user_id = #{userId} and follow_id = #{anotherUserId}
    </select>
    <select id="followingAuthors" resultType="string">
        select follow_id from follows
        where user_id = #{userId}
        and follow_id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    <select id="followedUsers" resultType="string">
        select follow_id from follows where user_id = #{userId}
    </select>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.favorites.infrastructure.mybatis.readservice.ArticleFavoritesReadService">
    <resultMap id="favoriteCount" type="io.spring.favorites.application.data.ArticleFavoriteCount">
        <constructor>
            <idArg column="id" javaType="string"/>
            <arg column="favoriteCount" javaType="integer"/>
        </constructor>
    </resultMap>
    <select id="isUserFavorite" resultType="java.lang.Boolean">
        select count(1) from article_favorites where user_id = #{userId} and article_id = #{articleId}
    </select>
    <select id="articleFavoriteCount" resultType="java.lang.Integer">
        select count(1) from article_favorites where article_id = #{articleId}
    </select>
    <select id="articlesFavoriteCount" resultMap="favoriteCount">
        select AF.article_id as id, count(AF.user_id) as favoriteCount
        from article_favorites AF
        where AF.article_id in
        <foreach collection="ids" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
        group by AF.article_id
    </select>
    <select id="userFavorites" resultType="java.lang.String">
        select AF.article_id
        from article_favorites AF
        where AF.article_id in
        <foreach collection="ids" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
        and AF.user_id = #{viewerId}
    </select>
</mapper>

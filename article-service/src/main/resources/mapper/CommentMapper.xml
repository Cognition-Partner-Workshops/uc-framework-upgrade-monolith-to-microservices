<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.spring.articleservice.infrastructure.mybatis.mapper.CommentMapper">
    <insert id="insert">
        insert into comments(id, body, article_id, user_id, created_at, updated_at)
        values(#{comment.id}, #{comment.body}, #{comment.articleId}, #{comment.userId}, #{comment.createdAt}, #{comment.createdAt})
    </insert>

    <select id="findById" resultType="io.spring.articleservice.core.comment.Comment">
        select id, body, user_id as userId, article_id as articleId, created_at as createdAt
        from comments
        where article_id = #{articleId} and id = #{id}
    </select>

    <delete id="delete">
        delete from comments where id = #{id}
    </delete>
</mapper>

<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.article.infrastructure.mybatis.mapper.UserProfileMapper">
    <insert id="upsert">
        INSERT INTO user_profiles (id, username, bio, image)
        VALUES (#{id}, #{username}, #{bio}, #{image})
        ON CONFLICT(id) DO UPDATE SET
            username = #{username},
            bio = #{bio},
            image = #{image}
    </insert>
    <select id="findById" resultType="java.lang.String">
        SELECT id FROM user_profiles WHERE id = #{id}
    </select>
</mapper>
